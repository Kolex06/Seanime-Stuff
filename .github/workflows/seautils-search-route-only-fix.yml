name: Fix SeaUtils Search Route Only

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/seautils-search-route-only-fix.yml
      - .github/scripts/fix-seautils-search-route-only.js
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix-seautils:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Patch SeaUtils search route detection
        run: node .github/scripts/fix-seautils-search-route-only.js

      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add plugins/SeaUtils-Kolex06-Version.json marketplace.json README.md .github/workflows/seautils-search-route-only-fix.yml .github/scripts/fix-seautils-search-route-only.js
          git commit -m "Fix SeaUtils search route carousel detection"
          git push
