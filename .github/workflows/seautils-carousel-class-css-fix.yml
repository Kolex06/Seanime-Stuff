name: Fix SeaUtils Carousel Class CSS

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/seautils-carousel-class-css-fix.yml
      - .github/scripts/fix-seautils-carousel-class-css.js
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix-seautils:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Patch SeaUtils carousel class CSS
        run: node .github/scripts/fix-seautils-carousel-class-css.js

      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add plugins/SeaUtils-Kolex06-Version.json marketplace.json README.md .github/workflows/seautils-carousel-class-css-fix.yml .github/scripts/fix-seautils-carousel-class-css.js
          git commit -m "Fix SeaUtils carousel class CSS"
          git push
