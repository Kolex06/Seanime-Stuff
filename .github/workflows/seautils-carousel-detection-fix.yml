name: Fix SeaUtils Carousel Detection

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/seautils-carousel-detection-fix.yml
      - .github/scripts/fix-seautils-carousel-detection.js
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix-seautils:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Patch SeaUtils carousel detection
        run: node .github/scripts/fix-seautils-carousel-detection.js

      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add plugins/SeaUtils-Kolex06-Version.json marketplace.json README.md .github/workflows/seautils-carousel-detection-fix.yml .github/scripts/fix-seautils-carousel-detection.js
          git commit -m "Fix SeaUtils carousel detection"
          git push
